{% set countComments = post.comments | length %}
{% for comment in post.comments %}
	{% if (offset is defined and comment.id > offset) or offset is not defined %}
		{% set cssComment = loop.index <= countComments - 3 and mostrarComentariosTodos is not defined ? 'Comentario Hidden' : 'Comentario'  %}
		{% include 'MondelCuentaloBundle:Comentario:mostrar.html.twig' %}						
	{% endif %}
{% endfor %}