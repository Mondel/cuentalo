
    <form id="registroForm" action="{{ path('registro') }}" method="post" {{ form_enctype(form) }}>

        {{ form_errors(form) }}

        {{ form_rest(form) }}

        <input type="button" value="Registrarme" onclick="validaRegistro()" />

    </form>