{% set sessionError = app.session.getFlash('error') %}
{% set sessionNotice = app.session.getFlash('notice') %}

{% if sessionError != '' %}

    <section class="alert error">{{ sessionError }}</section>

{% endif %}

{% if sessionNotice != '' %}

    <section class="alert notice">{{ sessionNotice }}</section>

{% endif %}